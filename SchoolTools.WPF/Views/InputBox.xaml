<Window 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Class="PolyhydraGames.SchoolTools.WPF.Views.MarginsDialogBox" 
    Title="Margins"
    Height="190"
    Width="300"
    MinHeight="10"
    MinWidth="300"
    ResizeMode="CanResizeWithGrip"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner" 
    FocusManager.FocusedElement="{Binding ElementName=leftMarginTextBox}">

 


        <!-- Accept or Cancel -->
        <StackPanel  >
            <TextBlock   TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" />
<TextBox Height="Auto" TextWrapping="Wrap" Text="This is a bunchof text I am testing out to see if the panel wraps or wat might happen instead."/>
            <Button Name="okButton" IsDefault="True">OK</Button>
            <Button Name="cancelButton" IsCancel="True" VerticalAlignment="Bottom">Cancel</Button>
            
        </StackPanel>
 

</Window>